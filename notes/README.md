# notes

Working material, not deliverables.

- `agent-report-01..03.md` — the agent's own assumptions, uncertainties, and "noticed but
  left alone" lists after each build step, requested in that shape by `CLAUDE.md`.
- `design-plan.md` — the visual design plan, written and committed before the build.
- `e2e-run-1.txt`, `e2e-run-2.txt` — two consecutive ingestion cycles against the live feed.
  The first alerts, the second does not. That contrast is the deduplication evidence.
- `running-log.md` — scratch observations captured during the build.
- screenshots — the admin view populated and empty, at desktop width and at 380px.